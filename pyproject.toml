[project]
name = "nanogpt"
version = "0.1.0"
description = ""
authors = [{ name = "Your Name", email = "you@example.com" }]
readme = "README.md"
requires-python = ">=3.10,<4.0"
dependencies = [
    "llm-foundry @ git+https://github.com/TheRootOf3/llm-foundry.git@dodo_v0.3.1",
    "mosaicml @ git+https://github.com/TheRootOf3/composer.git@v0.3.1",
    "torch",
    "numpy",
    "transformers",
    "datasets",
    "tiktoken",
    "wandb",
    "tqdm",
    "notebook",
    "matplotlib",
    "latex (>=0.7.0,<0.8.0)",
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
